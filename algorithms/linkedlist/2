# Problem:
# Given a linked list, remove the nth node from the end of list and return its head.
# 
# For example,
# Given linked list: 1->2->3->4->5, and n = 2.
# 
# After removing the second node from the end, the linked list becomes 1->2->3->5.
# 
# Note:
# Given n will always be valid.
# Try to do this in one pass.
#
# Idea:

class ListNode:
    def __init__(self, x):
        self.val = x
        self.next = None

class RemoveNthFromEnd:
    # @return a ListNode
    def removeNthFromEnd(self, head, n):
        pre = ListNode(0)
        pre.next = head 
        start = None 
        cursor = head
        counter = 0
        while cursor.next is not None:
            counter += 1
            if counter >= n:
                if counter == n :start = head;continue
                start = start.next
                pre = pre.next
            cursor = cursor.next
        if counter >=n:
            if pre.next is head : return head.next
            pre.next = start.next
        return head

a = ListNode(1)
b = ListNode(2)
c = ListNode(3)
a.next = b
b.next = c
RemoveNthFromEnd().removeNthFromEnd(a,1)
while a is not None:
    print a.val
    a = a.next
